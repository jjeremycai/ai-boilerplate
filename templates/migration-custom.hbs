-- Migration: {{name}}
-- Created: {{timestamp}}

-- Add your custom SQL statements here
-- This can include:
-- - Complex data transformations
-- - Multiple table operations
-- - Stored procedures (if supported)
-- - Data seeding
-- - Index optimization

-- Example data seeding:
-- INSERT INTO users (id, name, email) VALUES
--   ('user1', 'John Doe', 'john@example.com'),
--   ('user2', 'Jane Smith', 'jane@example.com');

-- Example cleanup:
-- DELETE FROM old_table WHERE status = 'inactive';
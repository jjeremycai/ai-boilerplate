-- Migration: {{name}}
-- Created: {{timestamp}}

-- Add your ALTER TABLE statements here
-- Examples:

-- Add a new column
-- ALTER TABLE existing_table ADD COLUMN new_field TEXT;

-- Create a new index
-- CREATE INDEX idx_table_field ON existing_table(field);

-- Note: SQLite has limited ALTER TABLE support
-- For complex changes, you may need to:
-- 1. Create new table with desired schema
-- 2. Copy data from old table
-- 3. Drop old table
-- 4. Rename new table
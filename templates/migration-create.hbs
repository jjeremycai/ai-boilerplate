-- Migration: Create {{name}}
-- Created: {{timestamp}}

CREATE TABLE {{snakeCase name}} (
  id TEXT PRIMARY KEY,
  -- Add your columns here
  -- Example: name TEXT NOT NULL,
  -- Example: email TEXT UNIQUE NOT NULL,
  -- Example: status TEXT DEFAULT 'active',
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

-- Add indexes if needed
-- CREATE INDEX idx_{{snakeCase name}}_status ON {{snakeCase name}}(status);
-- CREATE INDEX idx_{{snakeCase name}}_created_at ON {{snakeCase name}}(created_at);